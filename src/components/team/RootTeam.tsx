import React, { FunctionComponent } from 'react';

interface OwnProps {}

type Props = OwnProps;

const RootTeam: FunctionComponent<Props> = (props) => {

  return (<div>

      root team
  </div>);
};

export default RootTeam;
