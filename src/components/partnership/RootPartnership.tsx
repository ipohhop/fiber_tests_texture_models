import React, {FunctionComponent} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const RootPartnership: FunctionComponent<Props> = (props) => {

    return (

        <div>
            RootPartnership
        </div>

    );
};

export default RootPartnership;
